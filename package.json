{
  "name": "postic-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "playwright": "^1.42.0"
  }
}
