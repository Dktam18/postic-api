{
  "name": "postic-api",
  "version": "1.0.0",
  "description": "Smart Import API for Postic",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "playwright": "^1.42.0"
  }
}
